'use client';

import React from 'react';

interface CallManagerProps {
  currentUserId: string;
}

/**
 * CallManager is temporarily disabled to resolve React hooks ordering errors.
 * Voice and Video calling features will be restored in a future update.
 */
export const CallManager: React.FC<CallManagerProps> = () => {
  return null;
};
